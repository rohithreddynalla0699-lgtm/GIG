
  # GIG

  This is a code bundle for GIG. The original project is available at https://www.figma.com/design/0wae6eU3qVKubVKHHvKyAm/GIG.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  